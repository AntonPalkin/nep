<%= form_for(@type_io) do |f| %>
  <% if @type_io.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_io.errors.count, "error") %> prohibited this type_io from being saved:</h2>

      <ul>
      <% @type_io.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Название" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Описание" %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit "Сохранить" %>
  </div>
<% end %>
